import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class AccelerateForwardTest {

	
		public AccelerateForward state = new AccelerateForward();

		@Test
		void testAccelerateForward() {
			
			assertEquals("Accelerate Forward State",state.toString());
		}

	}
